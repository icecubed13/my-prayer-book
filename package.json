{
  "name": "my-prayer-book-cloudflare",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:cloudflare": "node scripts/prepare-wrangler.mjs && wrangler deploy --config .wrangler.deploy.jsonc",
    "db:setup": "wrangler d1 execute siddur-sync --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
